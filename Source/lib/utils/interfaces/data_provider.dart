// import 'package:api_requests/utils/models/url/url_resource.dart';
// import 'package:api_requests/utils/models/url/url_section.dart';

abstract class DataModel {}

abstract class DataProviderFactory<T> {
  // // * URLS
  // Future<List<UrlSection>> urlsList();
  // Future<UrlResource> urlResource(int id);
  // Future<void> saveUrlResource(UrlResource urlItem);

  // // * SETTINGS
  // Future<T> settings<T>(String name);
  // Future<T> settingsJson<T>(String name, T factory(Map<String, dynamic> data));
  // Future<void> setSettings(String name, String value);
  // Future<void> setSettingsJson(String name, dynamic value);
}
